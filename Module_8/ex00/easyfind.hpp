#ifndef EASYFIND_HPP
# define EASYFIND_HPP

# include <iostream>
# include <string>
# include <algorithm>
// # include <list>
// # include <map>

template <class T>
T &easyfind(T &container, int toFind)
{
	if (std::find(container.begin(), container.end(), item) != container.end())
	
}

#endif